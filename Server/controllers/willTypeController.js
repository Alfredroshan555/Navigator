// Functions to perform actions (login/signup/listing/searching ...)
export const getAllWillTypes = (req, res) => {
  return res.json({ msg: "All Will Types" });
};

export const getUser = (req, res) => {
  return res.json({ msg: "This is a user" });
};
